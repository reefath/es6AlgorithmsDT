/*
    code to demonstrate the outputting formats
*/
"use strict";
function toStringArr() {
    let arr= ['a', 'b', 'c'];
    console.log(arr.toString());
    console.log(arr.join('-'));
    
}

toStringArr();